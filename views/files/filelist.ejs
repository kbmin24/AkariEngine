<!-- deprecated: this file is no longer used. -->
There are <%= count %> files.
<ul>
    <% for(let i = 0; i < count; i++) { %>
        <li><a href='/file/<%= files[i].filename %>'><%= files[i].filename %></a></li>
    <% } %>
</ul>