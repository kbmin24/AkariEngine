<p>오늘 가장 많이 조회된 문서 30개입니다.</p>
<div style='overflow-x: auto'>
    <table class="table">
        <thead>
            <tr>
                <th scope='col'>#</th>
                <th scope='col'>문서명</th>
                <th scope='col'>조회수</th>
            </tr>
        </thead>
        <tbody>
            <% rank.forEach((rec, i, arr) => { %>
                <tr>
                    <td><%= i * 1 + 1 %></td>
                    <td><a href='/w/<%= rec.title %>'><%= rec.title %></a></td>
                    <td><%= rec.count %></td>
                </tr>
            <% }) %>
        </tbody>
    </table>
</div>