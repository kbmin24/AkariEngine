<p class='fw-bold'>Looking for <a href='/w/<%= searchtitle %>'>direct link</a>?</p>
<ul class="list-group list-group-flush">
    <% for (let i = 0; i < results.length; i++) { %>
        <li class="list-group-item">
            <a href='<%= results[i].title %>'><%= results[i].title %></a>
        </li>
    <% } %>
</ul>